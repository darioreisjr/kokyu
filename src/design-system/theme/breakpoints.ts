import type { BreakpointsOptions } from '@mui/material/styles';

import { breakpoints } from '../tokens/primitives/breakpoints';

export const kokyuBreakpoints: BreakpointsOptions = {
  values: { ...breakpoints },
};
