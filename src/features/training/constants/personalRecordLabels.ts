import type { PersonalRecordType } from '../types';

export const personalRecordTypeLabels: Record<PersonalRecordType, string> = {
  maxWeight: 'Maior carga',
  maxReps: 'Mais repetições',
  maxVolume: 'Maior volume',
  bestEstimatedOneRepMax: '1RM estimado',
  repPR: 'PR por repetições',
};
