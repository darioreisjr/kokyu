/** Standard Olympic bar — matches the spec's own "BAR CONFIG" example; user can override in preferences. */
export const DEFAULT_BAR_WEIGHT_KG = 20;

/** Matches the spec's own "AVAILABLE PLATES" example list, heaviest first. */
export const DEFAULT_AVAILABLE_PLATES_KG = [25, 20, 15, 10, 5, 2.5, 1.25];

export const BAR_WEIGHT_PRESETS_KG = [20, 15];
